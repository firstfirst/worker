import 'package:workedclick/core/shared-provider.dart';

class ExamLoading extends StatelessWidget {
  const ExamLoading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
